from logging import getLogger

dispatcher = getLogger("maxo.dispatcher")
long_polling = getLogger("maxo.long_polling")
webhook = getLogger("maxo.webhook")
utils = getLogger("maxo.utils")
bot_session = getLogger("maxo.bot.session")
